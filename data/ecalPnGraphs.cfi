module ecalPnGraphs = EcalPnGraphs {

	string digiProducer     = 'ecalEBunpacker'

	# requested FEDs
	untracked vint32 requestedFeds = {-1}

	# requested EBs 
	untracked vstring requestedEbs = {"none"}

	# list of Pns to be graphed around
        untracked vint32  listPns = { 5 }	

	# length of the line centered on listPns containing the Pns you want to see
        # needs to be an odd number
       	untracked int32  numPn = 9
        untracked string fileName =  'test.root' 	

	
}
